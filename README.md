# jupyter-on-docker-with-mecab-neologd

Setup a Docker image that enables MeCab + NEologd on JupyterLab.

## Build and Run

```sh
docker-compose up jupyter --build
```

Then, navigate to http://localhost:8888/
